apiVersion: 1

datasources:
  - name: Postgres
    type: postgres
    url: postgres:5432
    user: yourusername
    secureJsonData:
      password: 'yourpassword'
    jsonData:
      database: mqtt
      sslmode: 'disable' # disable/require/verify-ca/verify-full
  